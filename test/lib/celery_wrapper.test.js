/* global __ROOT_DIR */
require(`${__ROOT_DIR.TEST_SUPPORT}/_all.js`);

// let rewire        = require('rewire');
// let expect        = require('expect.js');
// let sinon         = require('sinon');
//
// let celeryWrapper = rewire(`${__ROOT_DIR.LIB}/celery_wrapper.js`);


describe('CeleryWrapper', function() {
  xit("celery connects to redis", function() {
  });

  xit("celery receives errors", function() {
  });
});
