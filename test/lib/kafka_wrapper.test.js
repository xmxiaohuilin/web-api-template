/* global __ROOT_DIR */
require(`${__ROOT_DIR.TEST_SUPPORT}/_all.js`);

// let rewire = require('rewire');
// let kafka_wrapper = rewire(`${__ROOT_DIR.LIB}/kafka_wrapper`);
// let sinon = require('sinon');

describe('KafkaWrapper', function(){
  xit('creates client with zookeeper url', function() {

  });

  xit('creates a producer with connceted client', function() {

  });
});
